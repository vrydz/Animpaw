rules_version = '2';
service cloud.firestore {
  match /databases/{database}/documents {
    match /{document=**} {
      // Game state is authoritative on the server. Direct client Firestore
      // access is forbidden until collection-specific ownership rules exist.
      allow read, write: if false;
    }
  }
}
